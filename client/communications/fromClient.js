
// var form = document.getElementById('form');
// var input = document.getElementById('input');

export default function fromClient(socket) {

  // form.addEventListener('submit', function (e) {
  //   e.preventDefault();
  //   if (input.value) {
  //     socket.emit('chat message', input.value);
  //     input.value = '';
  //   }
  // });


}